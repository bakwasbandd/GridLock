# GridLock
Gridlock is a small Python interpreter for a line-based game DSL. A `.grid` file describes a grid, places entities such as the player, enemies, walls, and the goal, then runs the result as an interactive terminal game.
